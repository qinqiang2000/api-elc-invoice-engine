<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dynamic">
  <!-- 动态SQL查询 -->
  <select id="dynamicSelect" resultType="map" parameterType="map">
    ${sql}
  </select>

  <select id="dynamicSelectOneField" resultType="string" parameterType="com.kingdee.fpy.service.cel.QueryParams">
    ${sql}
  </select>

  <!-- MySQL版本获取所有表名 -->
  <select id="getAllTables" resultType="string">
      SELECT TABLE_NAME
      FROM INFORMATION_SCHEMA.TABLES
      WHERE TABLE_SCHEMA = DATABASE()
  </select>
</mapper>